import SessionWrapper from "../components/SignIn";

export default function LoginPage() {
    return (
        <div className="flex items-center justify-center h-screen">
            <SessionWrapper />
        </div>
    );
}